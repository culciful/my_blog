# culciful blog

- `vue3-ts-blog/`     前端（Vue 3 + TypeScript + Vite）
- `springboot3-blog/` 后端（Spring Boot 3 + MyBatis-Plus + MySQL）
- `db/`               建库脚本 `blog.sql`、接口文档 `api-docs.md`、`docker-compose.yml`

本地整套起来：`cd db && cp .env.example .env`（填密码和 JWT 密钥）`&& docker compose up --build`
