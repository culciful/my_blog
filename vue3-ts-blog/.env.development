# npm run dev 使用（Vite mode: development）
# 是否启用本地 JSON mock（true 时注入 mockServer，且 axios 使用相对路径）
VITE_USE_MOCK=true
# 开发直连后端根地址；留空则走相对路径 + vite.config 中的 dev 代理
VITE_API_BASE=
# 开发代理转发的后端地址
VITE_PROXY_TARGET=http://127.0.0.1:8080
